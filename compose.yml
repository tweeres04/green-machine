services:
  teamstats:
    build:
      dockerfile: ./Dockerfile
      context: .
    restart: always
    volumes:
      - ./database:/app/database
    env_file:
      - .env
    environment:
      TZ: America/Vancouver

networks:
  default:
    external: true
    name: proxy
